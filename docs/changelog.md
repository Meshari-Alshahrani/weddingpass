# 📜 سجل التغييرات وقرارات المعمارية التاريخية (Changelog & ADRs)
### منظومة WeddingPass - سجل التطوير الشامل والقرارات المعمارية الموثقة

---

## 🏷️ سجل الإصدارات التاريخي الكامل (Changelog)

---

### 🔹 [v6.0.0] - 2026-08-24 (Server-Side Admin Auth, Provisional Offline & Registration Integrity)

إصدار التحصين والسلامة الكبرى: إغلاق أخطر ثغرتين (لوحة إدارة مكشوفة، أوفلاين غير موثوق) ومواءمة قاعدة البيانات مع منطق الأعمال على مستوى الـschema.

* **مصادقة الإدارة Server-Side (P0 — ADR-029/032):**
  - حماية حقيقية لـ`/admin` عبر DAL‏ (`requireAdminSession`) تسبق أي استعلام؛ بيانات الضيوف لم تعد تصل إلى RSC payload بدون جلسة موثقة.
  - مسار `/api/admin/auth` جديد: ‏`ADMIN_PIN` env مستقل وإلزامي بالإنتاج (مع guard يمنع تطابقه مع gate/supervisor)، كوكي `__Host-admin_session` ‏HttpOnly/Secure/Strict/4h دون توكن في JSON، وDELETE للخروج.
  - `proxy.ts` (Middleware الجديد في Next 16): فحص تفاؤلي مبكر + إعادة توجيه؛ الحماية الفعلية تبقى في الـDAL وكل handler.
  - تفكيك القفل الوهمي: حذف شاشة PIN العميلة، وsessionStorage، **والباكدور `'2026'`** من AdminDashboard.
  - ربط الجلسة بالفعالية: كل عملية في `/api/admin` تتحقق `session.eventId === event.id`.
  - إغلاق الباب التلقائي التطويري: لا admin ضمنياً إلا مع `WEDDINGPASS_ALLOW_MOCK=true`.

* **الأوفلاين Provisional Admission (P0 — ADR-030):**
  - إصلاح مطابقة الأوفلاين المكسورة سابقاً: هاش SHA-256 محلي عبر Web Crypto يقارن ضد كاش البوابات (كانت المقارنة السابقة مستحيلة رياضياً).
  - نموذج صادق: `LOCAL_ADMISSION` «دخول مؤقت بانتظار المزامنة» بدل ادعاء تحقق نهائي؛ localStorage ليس مصدر حقيقة.
  - طابور مزامنة بمعرّف فريد `queueId` (idempotency key) + `deviceId/sequenceNo/queuedAt`؛ الحذف فقط عند حكم نهائي من السيرفر، والتعارضات (`ALREADY_CHECKED_IN` من بوابة أخرى) تظهر كـ⚠️ لا ✅.
  - `process_secure_checkin` يعيد النتيجة الأصلية الكاملة عند إعادة الإرسال بنفس queueId (الفهرس الفريد الجزئي حاجز سباق فقط).

* **سلامة التسجيل وقاعدة البيانات (P0/D — ADR-030/031) عبر migration ‏`007_registration_integrity.sql`:**
  - `ALREADY_REGISTERED` بدون أي تدوير أو إبطال للبطاقة القائمة (منع DoS بإبطال QR ضيف شرعي برقم جواله).
  - Clamp مقاعد الضيف بـ`max_seats_per_guest` داخل الـRPC (كان موجوداً في الوهمي فقط!).
  - فهرس جزئي UNIQUE ‏`(event_id, group_link_id, primary_phone)` + CHECKs للأعداد و`confirmed<=allowed` وتنسيق الرقم المطبع `^9665\d{8}$` — جميعها بنمط **فحص مسبق ثم ABORT بتقرير تشخيصي** بدل dedupe صامت.
  - DROP الصريح لتوقيع الدالة الثماني قبل التعريف العشري (منع overload يتيمة مكشوفة).
  - تهاني تسجيل القروبات والحجر الصحي: `wishes.is_approved` افتراضاً `false` (اتساق ADR-005).
  - `search_parties` جديدة: RPC معاملاتي ‏SECURITY INVOKER بتهريب LIKE يحل محل تجميع `.or()` النصي (إغلاق حقن فلاتر PostgREST) مع دعم الصيغتين المحلية/الدولية للجوال.

* **حدود البيانات والاعتماد (ADR-034):**
  - إعادة تسمية `GuestEntryPassCredential`: التوكن الخام bearer credential يمر عبر قناتين موثقتين فقط (الإصدار، والاسترجاع بحيازة رابط الدعوة) ويُحظر في admin/logs/cache/errors.
  - `/api/public/moment` يعيد DTO بلا `uploader_phone`؛ حذف QR fallback القابل للتخمين `wp_pass_${party.id}` واستبداله بحالة «بطاقة غير مكتملة».

* **Hardened CSP (ADR-033):** nonce + strict-dynamic عبر proxy مع `CSP_MODE=off|report-only|enforce` (الافتراضي report-only للانتقال الآمن)، واستثناء موثق لصفحة stress-test الثابتة. ترحيل الخطوط إلى `next/font/google` (استضافة ذاتية — لا اعتماد خارجي يوم الحفل).

* **تنظيف التشفير:** حذف fallbacks الضعيفة (`Math.random` وهاش 32-bit المزيّف) لصالح رمي أخطاء صريحة؛ `fixedTimeEqualHex` باسم صادق بلا ادعاء timing-guarantee بالمتصفح.

* **الاختبارات والتوثيق:** ‏qa_runner ‏60 فحصاً (+توازي هاش العميل/الخادم، ICS octets)، حارس `database_integrity_test` جديد داخل npm test، توسيع boundary guards (ترتيب DAL-before-query، حظر '2026'، DTO اللحظات)، عقود `/api/admin/auth`، ‏E2E ‏Playwright ‏(smoke ‏CSP/hydration على 5 مسارات + دورة QR كاملة حتى ALREADY_CHECKED_IN)، توحيد sanitize/ICS/FALLBACK_EVENT في مصادر واحدة، وحذف `qa_test_suite.ts` اليتيم.

> متطلبات النشر: تطبيق migration 007 (يفشل عمداً مع تقرير إن وُجدت بيانات مكررة/غير مطبعة تتطلب قراراً بشرياً)، ضبط `ADMIN_PIN`، ثم `npm run test:supabase`، ولاحقاً قلب `CSP_MODE=enforce` بعد نجاح E2E. البوابة الأخيرة: عرض تشغيلي كامل على staging (500–1000 ضيف وجهازا بوابة مع قطع اتصال مقصود).

---

### 🔹 [v5.9.4] - 2026-08-23 (Production Fail-Closed & Deployment Verification)

* **Fail-Closed فعليًا:** حظر `MockRepository` في الإنتاج عند غياب URL أو service-role key الخاصين بـSupabase، وحصره على الاختبارات أو تطوير محلي صريح عبر `WEDDINGPASS_ALLOW_MOCK=true`.
* **إزالة fallbacks الإنتاجية:** لم يعد `SupabaseRepository` ينشئ فعالية افتراضية أو يعيد بيانات بديلة عند أخطاء الإنتاج؛ التهيئة مسؤولية migrations/seed منفصل.
* **حماية إعدادات النشر:** إضافة [`.env.example`](../.env.example) وتوثيق أسرار Supabase والجلسات وRedis/KV، مع اشتراط تحديد وضع الـRate Limiter في الإنتاج.
* **فحص صحة حقيقي:** مسارا الصحة الآن يتحققان من اتصال Supabase server-side، وتوجد حزمة `npm run test:supabase` غير معدِّلة للبيانات لفحص الاتصال وRPC الفعليين بعد ضبط الأسرار.
* **مصدر مخطط واحد:** تعطيل نسخ SQL المجمعة القديمة، واعتماد `supabase/migrations/` وحده كمصدر للمخطط والتغييرات الأمنية.
* **ضبط إصدار وتوثيق:** توحيد الإصدار على `5.9.4` في الحزمة ومخارج الصحة والتوثيق، وإزالة أعداد المسارات/الاختبارات الثابتة التي كانت تنجرف عن الكود.

> لم يُشغّل اختبار Supabase الحقيقي ضمن هذا الإصدار لأنه يتطلب أسرار المشروع؛ يجب تشغيله في بيئة النشر بعد تطبيق migrations.

---

### 🔹 [v5.9.2] - 2026-08-23 (Critical Security Hardening & Zero-Trust Production Seal)
* **سد الثغرات وتوحيد قاعدة البيانات والأمان الموزع (Enterprise Production Seal):**
  - **ملف تهجير صريح وآمن لحذف السياسات القديمة (`006_remove_public_insert_policies.sql`):** تنفيذ حذف سياسات الإدخال العام فقط بعد التحقق من وجود الجدول، مع دعم أسماء السياسات القديمة ومنع توقف الترحيل على جداول legacy غير موجودة.
  - **توحيد جدول اللحظات في Supabase (`moments` Table Consistency):** تصحيح الاستعلامات في [`SupabaseRepository.ts`](file:///Users/ewexox/Documents/weddingpass/lib/repositories/supabase/SupabaseRepository.ts) وتوحيد القراءة والإضافة والحذف والاعتماد على جدول `moments` الموحد بدلاً من الخلط مع `event_moments`.
  - **الفحص الإلزامي الشامل للصور (URLs & Base64 Magic Bytes):** توسيع [`lib/security/imageValidation.ts`](file:///Users/ewexox/Documents/weddingpass/lib/security/imageValidation.ts) بدالتي `validateImageUrl` و `validateImagePayload` لحظر امتدادات السكربتات والملفات التنفيذية (`.php`, `.js`, `.svg`, `.exe`, `.sh`) وفرض استضافة الصور على سحابات معتمدة أو امتدادات صور قياسية، بالإضافة لفحص البايتات الثنائية للـ Base64.
  - **تطوير محرك الـ Rate Limiter للتوزيع السحابي والـ Edge:** اعتماد تكامل Upstash Redis / Vercel KV REST في كافة المسارات الحساسة، مع fallback محلي وتقييد الذاكرة المحلية (Max 10k Keys).
  - **اختبارات عقد المسارات (`tests/api_routes_integration_test.mjs`):** فحص عقود HTTP والحماية من انحراف كود المسارات والإعدادات الأمنية؛ ولا تُعد بديلًا عن اختبار نشر حي على Supabase.
  - **سد ثغرة تسريب كاش البطاقات (P0 Fix):** إزالة معامل `offlineCache=true` تماماً من المسار العام [`/api/join`](file:///Users/ewexox/Documents/weddingpass/app/api/join/route.ts)، وحصر تصدير كاش البطاقات المشفرة حصرياً على المسار المحمي [`/api/gate/cache`](file:///Users/ewexox/Documents/weddingpass/app/api/gate/cache/route.ts).
  - **الحماية الحصرية بكوكيز `HttpOnly` ومنع سرقة الجلسات (P1 Fix):** حظر إعادة توكن الجلسة المشفر في جسم الـ JSON من [`/api/gate/auth`](file:///Users/ewexox/Documents/weddingpass/app/api/gate/auth/route.ts) ومنع حفظه في `sessionStorage` في [`GateScanner.tsx`](file:///Users/ewexox/Documents/weddingpass/components/GateScanner.tsx).

---

### 🔹 [v5.9.1] - 2026-08-22 (Production Cloud Hardening, Master Admin PIN Lock & WhatsApp Unicode Sanitization)
* **الأمان السحابي المشدد وتجربة الإنتاج:**
  - **حماية لوحة التحكم الإدارية بشاشة قفل PIN مشفرة (`/admin`):** إضافة شاشة قفل ملكية تمنع أي وصول غير مصرح لبيانات الضيوف والإعدادات، وتتطلب إدخال رمز المرور الإداري المخصص (الافتراضي `2026` مع إمكانية تغييره)، مع حفظ الجلسة في `sessionStorage` وإضافة زر "قفل اللوحة 🔒" لتسجيل الخروج الفوري.
  - **التهيئة والزرع الذاتي لقاعدة البيانات السحابية (Zero-Crash Auto-Seeding):** تحديث `SupabaseRepository` لاستخدام الاستعلامات الآمنة والزرع التلقائي للفعالية الافتراضية عند تهيئة قاعدة بيانات Supabase جديدة دون الحاجة لإجراءات يدوية معقدة.
  - **التطهير والتشفير القياسي لروابط الواتساب (WhatsApp Unicode Sanitization):** إزالة كافة الإيموجيات المركبة ورموز الـ ZWJ التي كانت تسبب ظهور علامات الاستفهام السوداء (``) في أجهزة أندرويد، واستبدالها بإيموجيات قياسية موحدة عالمياً (`💍`, `🌹`, `🔗`).
  - **تحصين مكونات السيرفر (Server Component Hardening):** تغليف صفحات SSR بـ `Promise.all` ومعالجة آمنة للأخطاء لضمان تحميل واجهات `/admin` و `/checkin` و `/admin/live` باستقرار 100%.
  - **إنشاء سكريبت الفحص السحابي الشامل (`scratch/cloud_live_tester.mjs`):** أداة فحص آلية لاختبار كافة سيناريوهات الحفل مباشرة عبر الإنترنت على سيرفر Vercel الحي.

---

### 🔹 [v5.9.0] - 2026-08-22 (Luxury UI/UX, Triple Font Hierarchy & WhatsApp Optimization)
* **ترقية تجربة المستخدم والهوية الملكية الفاخرة:**
  - **التوليفة الطباعية الثلاثية (Google Fonts):** اعتماد `Alexandria` للعناوين الملكية العريضة وأسماء العرسان، و `Amiri` للآيات والقصائد، و `IBM Plex Sans Arabic` للأرقام والبيانات وبطاقات الدخول.
  - **استراتيجية الواتساب المختصرة الملكية (The Prestigious Teaser):** تحديث قوالب رسائل الواتساب مع دعم الـ Unicode الفاخر (`﷽` و `*العريض*` والفواصل الملكية) لتحفيز فتح الرابط وتأكيد الـ RSVP.
  - **أبعاد صور Canva والتكبير الكامل (Lightbox Zoom):** دعم العرض الكامل لصور البطاقات (9:16) مع إمكانية النقر للتكبير بملء الشاشة.
  - **المشاركة الأصلية للآيفون (`Web Share API`):** حفظ البطاقة في ألبوم الصور والمشاركة في الواتساب عبر قائمة الآيفون الرسمية.
  - **هندسة منطقة الإبهام عند البوابات (Thumb-Zone Layout):** وضع أزرار الفلاش والبحث في الثلث السفلي لتسهيل الاستخدام بيد واحدة.
  - **زر التراجع السريع عند الخطأ (5-Second Undo Toast):** إشعار عائم لمدة 5 ثوانٍ عند الدخول للتراجع الفوري عن اللمسات الخاطئة.
  - **التغذية الصوتية والحسية (Audio & Haptics):** نغمات ترحيبية وملكية وتحذيرية عبر `Web Audio API` مع تفعيل `AudioContext.resume()` عند إدخال الـ PIN لدعم Safari، واهتزازات `navigator.vibrate`.
  - **وضع ملء الشاشة لشاشات القاعة (`LiveMonitor.tsx`):** زر تفعيل العرض الكامل للبروجكترات وشاشات العرض الكبرى.

---

### 🔹 [v5.8.0] - 2026-08-22 (Enterprise Production Architecture & OWASP/Supabase Standards)
* **المعمارية المؤسساتية والأمان الصارم (OWASP & Supabase 2026):**
  - **حظر الوصول العام لبيانات الضيوف في RLS (P0 Fix):** إلغاء كافة سياسات `Public read` على `parties` و `events`، وحصر الوصول المباشر لقاعدة البيانات عبر خادم Next.js بواسطة `service_role` فقط.
  - **إنشاء العرض الآمن للفعالية (`public_events_view`):** عزل `gate_pin` و `iban` و `bank_name` و `owner_id` عن الاستعلام العام، وإتاحة الحقول التجميلية للبطاقة فقط.
  - **قفل صلاحيات الـ RPC على `service_role` فقط (P0 Fix):** سحب صلاحية استدعاء `process_secure_checkin` من `anon` و `authenticated`، وتثبيت `SET search_path = ''` مع أسماء الجداول الكاملة لمنع هجمات الـ search_path.
  - **بناء معمارية الـ Repository Pattern الحقيقية (`lib/repositories/`):** عزل منطق الأعمال تماماً عن طبقة التخزين عبر واجهات `types.ts`، وتنفيذ `SupabaseRepository` بأسلوب **Fail-Closed** الصارم، مع حصر `MockRepository` على بيئة الاختبارات السريعة.
  - **المعاملات الذرية السحابية لـ RSVP والكوتا (`005_atomic_rsvp_and_group_rpc.sql`):** إنشاء دوال SQL ذرية `submit_party_rsvp_atomic` و `register_group_guest_atomic` بقفل السجلات `FOR UPDATE` لضمان تكامل المقاعد والتذاكر في معاملة ACID واحدة.
  - **اعتماد كوكيز OWASP `__Host-`:** استخدام بادئة `__Host-gate_session` مع `Secure; HttpOnly; SameSite=Strict; Path=/`.
  - **فصل الـ Health Check العام عن الإداري:** جعل `/api/health` مقتصراً على البنية التحتية، ونقل الإحصائيات وفحص الـ RPC والـ Latency إلى مسار محمي جديد `/api/admin/system-health`.
  - **دعم معايير مفاتيح Supabase لعام 2026:** دعم `SUPABASE_PUBLISHABLE_KEY` و `SUPABASE_SECRET_KEY`.

---

### 🔹 [v5.7.0] - 2026-08-22 (Real Production Data Layer & Fail-Closed Architecture)
* **الطبقة الإنتاجية الصارمة والأمان النهائي:**
  - **تطبيق معمارية Fail-Closed الصارمة بالإنتاج:** إلغاء استخدام الذاكرة المحلية المؤقتة (`Map()`) بالإنتاج ورمي خطأ صريح عند انقطاع الاتصال بقاعدة بيانات Supabase، مع حصر الـ in-memory mock على بيئة التطوير والاختبارات السريعة فقط.
  - **تصحيح صلاحيات Supabase JWT في `adminAuth.ts`:** ضبط `isOwner = false` كقيمة افتراضية لمنع منح صلاحية المالك تلقائياً للمستخدمين العاديين، وفرض التحقق من دور `admin` في الـ metadata أو ثبوت ملكية الفعالية في قاعدة البيانات.
  - **تحصين المسارات العامة وسد ثغرات انتحال الشخصية:** إزالة `partyId` من `/api/public/wish`، وإلزام قيود الأطوال القصوى للنصوص (80 حرفاً للاسم، 500 حرف للرسالة، 200 حرف لوصف الصورة، و 5 ميجابايت للصور).
  - **إنشاء نقطة الفحص الميداني المسبق (`/api/health`):** مسار صحة المنظومة لفحص قاعدة البيانات، والمفاتيح السرية، والـ RPC، وإحصائيات الحفل قبل انطلاق الفعالية.
  - **تنظيم ملفات الترحيل السحابي في `supabase/migrations/`:** تقسيم السكيما إلى ملفات ترحيل معيارية ومستقلة (`001_initial_schema.sql`, `002_rls_and_security.sql`, `003_atomic_checkin_rpc.sql`, `004_storage_and_realtime.sql`).

---

### 🔹 [v5.6.0] - 2026-08-22 (Master Production Hardening & Full Consensus)
* **المعمارية والأمان الصارم:**
  - **فرض الرفض 401 في `/api/checkin`:** حظر الوصول لمسار التحضير دون جلسة بوابة موثقة صالحة.
  - **التوقيع المشفر القياسي HMAC:** استخدام `crypto.createHmac('sha256', secret)` لتوقيع التوكنات.
  - **فصل `ADMIN_SECRET` عن مفاتيح Supabase:** عزل التوثيق الإداري لضمان حماية المسارات الحساسة.
  - **تأمين الجلسات بالكوكيز:** تعيين كوكيز `HttpOnly; Secure; SameSite=Strict` لمدة 4 ساعات.
  - **حصر تجاوز الأقسام على المشرف بالسيرفر:** التحقق من صلاحية `role === 'supervisor'` في الخادم وليس العميل.

---

### 🔹 [v5.5.0] - 2026-08-22 (Gate Security Hardening & Concurrency Resilience)
* **الأمان وحالات السباق التزامني:**
  - **سد ثغرة التحقق من PIN بالعميل:** نقل كافة عمليات التحقق من رمز البوابة إلى الخادم `/api/gate/auth`.
  - **إلغاء حقل role من طلب العميل:** تحديد الرتبة تلقائياً بناءً على الـ PIN المدخل (رمز المشرف -> supervisor، رمز البوابة -> operator).
  - **إنشاء حزمة اختبارات السباق التزامني (`tests/concurrency_test.mjs`):** محاكاة 100 طلب متزامن لاختبار القفل ومنع تكرار التحضير وتجاوز الكوتا.
  - **إضافة حقل `is_vip` الصريح:** دعم تصنيف كبار الشخصيات والتنبيهات الصوتية المخصصة.

---

### 🔹 [v5.4.0] - 2026-08-22 (Live Test Suite & Live Imports Integration)
* **الجودة والمطابقة المباشرة:**
  - **ربط ملف الاختبارات بالكود الحي (`tests/qa_runner.mjs`):** استيراد الدوال مباشرة من `lib/crypto/tokens.ts`، `lib/utils/phone.ts`، `lib/security/rateLimiter.ts`، و `lib/db/store.ts`.
  - **إصلاح دوال تطبيع الهاتف:** رفض الأرقام غير الصالحة بالكامل وحمايتها من هجمات ReDoS.
  - **توسيع الفحوصات المؤتمتة:** رفع عدد الاختبارات الناجحة إلى 40 فحصاً شاملاً.

---

### 🔹 [v5.3.0] - 2026-08-22 (Cybersecurity Threat Matrix & Defense-in-Depth)
* **التحصين السيبراني الشامل:**
  - بناء مصفوفة التهديدات السيبرانية (OWASP Threat Matrix).
  - إضافة حماية فخاخ البوتات (Honeypot Trap Fields).
  - فحص البايتات السحرية للصور (Magic Bytes Header Validation).
  - تحييد هجمات حقن صيغ الإكسل (CSV Formula Injection Sanitization).
  - حماية تبريكات القاعة من هجمات Stored XSS.

---

### 🔹 [v5.2.0] - 2026-08-21 (Group Links Quota & Multi-Host Architecture)
* **روابط المجموعات والداعين المتعددين:**
  - دعم روابط المجموعات العامة المخصصة لقروبات الواتساب مع وضع سقف محدد للكوتا (`unlimited`, `warning`, `strict`).
  - دعم تعدد الداعين (العريس، والد العريس، والد العروس، قسم النساء) وتوزيع المقاعد والإحصائيات بدقة.
  - إدارة الطاولات الفردية وبطاقات الدخول الذكية.

---

### 🔹 [v5.1.0] - 2026-08-21 (Realtime Hall Wishes & Live Screen Monitor)
* **التفاعل اللحظي وشاشة القاعة:**
  - شاشة المراقبة اللحظية `/admin/live` للبث المباشر لتبريكات الضيوف في صالة الحفل.
  - ألبوم الصور التشاركي `/moments` مع خوارزمية الحجر الصحي والموافقة الإدارية المسبقة.
  - دعم تنبيهات كبار الشخصيات والاحتياجات الخاصة (كراسي متحركة).

---

### 🔹 [v5.0.0] - 2026-08-20 (Initial Architectural Genesis & Core Next.js Engine)
* **التأسيس المعماري والنموذج الأولي:**
  - إنشاء هيكل المشروع باستخدام Next.js 16 App Router و Tailwind CSS و TypeScript.
  - تطبيق مبدأ فصل الرموز (Token Decoupling Principle) بين رمز الدعوة ورمز بطاقة الدخول.
  - تخزين التجزئات المشفرة SHA-256 للرموز وحظر تخزين النصوص الصريحة.
  - بناء واجهة الدعوة الفاخرة للضيوف مع مولد ملفات التقويم RFC 5545.

---

## 🏛️ سجل القرارات المعمارية الكامل (Architecture Decision Records - ADRs)

### ADR-001: فصل رموز الدعوة عن رموز بطاقات الدخول (Token Decoupling)
* **القرار:** استخدام رمز `wp_inv_...` لاستعراض الدعوة وتأكيد الحضور، وتوليد رمز منفصل تماماً `wp_pass_...` للـ QR Code بعد التأكيد.
* **السبب:** منع استخراج باركود الدخول قبل تأكيد الحضور، وتقليل احتمالية تزوير بطاقات الدخول.

### ADR-002: تخزين تجزئة SHA-256 للرموز وحظر التخزين الصريح
* **القرار:** تخزين `invitation_token_hash` و `pass_token_hash` فقط في قاعدة البيانات.
* **السبب:** ضمان أنه في حال تسرب قاعدة البيانات، لا يمكن للمخترق استخراج بطاقات QR صالحة لدخول الحفل.

### ADR-003: استخدام خوارزمية المقارنة ذات التوقيت الثابت (Constant-Time Compare)
* **القرار:** استخدام `crypto.timingSafeEqual` للتحقق من الرموز والتجزئات.
* **السبب:** منع هجمات قياس التوقيت (Timing Attacks) التي تستغل الفروق الزمنية في مقارنة النصوص.

### ADR-004: محرك تحديد معدل الطلبات بانزلاق زمني (Sliding-Window Rate Limiter)
* **القرار:** فرض قيود صارمة على عدد المحاولات لكل عنوان IP على المسارات الحساسة.
* **السبب:** حماية الخوادم من هجمات استنزاف الموارد (Denial of Wallet) ومنع هجمات التخمين.

### ADR-005: الحجر الصحي الإلزامي للصور المرفوعة (Media Quarantine Flow)
* **القرار:** تعيين `is_approved = false` افتراضياً لأي صورة يرفعها الضيوف في ألبوم الحفل.
* **السبب:** منع ظهور أي محتوى غير لائق على شاشات القاعة الكبرى إلا بعد اعتماد المنظم.

### ADR-006: العزل التبادلي لبوابات الأقسام (Cross-Section Gate Isolation)
* **القرار:** إطلاق تحذير أصفر `CROSS_SECTION_WARNING` عند مسح بطاقة نسائية عند بوابة الرجال أو العكس.
* **السبب:** احترام الخصوصية الاجتماعية والتنظيمية في الأعراس السعودية.

### ADR-007: توثيق الطوابع الزمنية المزدوجة لكاش الأوفلاين (Dual-Timestamp Offline Mesh)
* **القرار:** تسجيل `device_scanned_at` محلياً و `server_synced_at` عند المزامنة.
* **السبب:** ضمان دقة سجلات التدقيق ومنع التلاعب بالتوقيت عند التحضير في وضع عدم الاتصال.

### ADR-008: دعم الروابط العامة لقروبات الواتساب مع كوتا مرنة (Group Quota Governance)
* **القرار:** دعم روابط تسجيل أعضاء المجموعات مع خيارات تحكم مرنة بسقف الحضور (`unlimited`, `warning`, `strict`).
* **السبب:** تسهيل دعوة الأقارب وزملاء العمل مع ضبط الطاقة الاستيعابية للقاعة.

### ADR-009: تحصين ملفات الإكسل ضد هجمات الحقن (CSV Formula Neutralization)
* **القرار:** إضافة بادئة علامة الاقتباس المفردة `'` لأي حقل يبدأ بـ `=`, `+`, `-`, `@` عند تصدير الكشوفات.
* **السبب:** حماية أجهزة المنظمين من هجمات تنفيذ الأوامر التلقائية في برامج جداول البيانات.

### ADR-010: الفحص المسبق لترويسة الملفات الثنائية (Magic Bytes Image Verification)
* **القرار:** فحص البايتات السحرية الحقيقية (RIFF, JPEG, PNG) بدلاً من الاعتماد على الامتداد الظاهري.
* **السبب:** منع رفع الملفات الخبيثة أو السكربتات المتنكرة بصيغة صور.

### ADR-011: التحقق الإلزامي من رمز PIN بالسيرفر (Server-Side PIN Verification)
* **القرار:** إلغاء التحقق من رمز PIN في كود العميل ونقله بالكامل لمسار `/api/gate/auth`.
* **السبب:** منع كشف الرمز في أدوات المتصفح (DevTools) ومنع التجاوز غير المصرح للبوابات.

### ADR-012: تحديد رتبة الجلسة حتمياً في السيرفر (Deterministic Role Derivation)
* **القرار:** إلغاء استقبال حقل `role` في جسم الطلب وتحديده حصراً بمقارنة الـ PIN في الخادم.
* **السبب:** سد ثغرة تصعيد الصلاحيات التلقائي لرتبة مشرف.

### ADR-013: توقيع جلسات البوابة بـ HMAC-SHA256 المشفر (HMAC Signed Gate Sessions)
* **القرار:** توقيع جلسات البوابة باستخدام سر السيرفر `GATE_SESSION_SECRET` ورفض أي جلسة تالفة.
* **السبب:** منع تزوير الجلسات وحماية المنظومة من هجمات التلاعب بالبيانات.

### ADR-014: الرفض الإلزامي 401 عند غياب جلسة البوابة (Mandatory Gate Session 401)
* **القرار:** حظر مسار `/api/checkin` وإرجاع `401 Unauthorized` فوراً عند عدم وجود جلسة معتمدة.
* **السبب:** ضمان عدم استدعاء مسار التحضير إلا من محطات البوابات الموثقة.

### ADR-015: حماية جلسات المشرفين بكوكيز آمنة (HttpOnly Secure Session Cookies)
* **القرار:** تخزين توكن الجلسة في كوكيز `HttpOnly; Secure; SameSite=Strict` لمدة 4 ساعات.
* **السبب:** حماية التوكن من السرقة عبر ثغرات XSS وحظر تخزينه في `sessionStorage`.

### ADR-016: حوكمة التجاوز الاستثنائي للقسم بالسيرفر (Server-Enforced Supervisor Override)
* **القرار:** حظر معامل `forceCrossSection` في السيرفر إلا إذا كانت الجلسة برتبة `supervisor`.
* **السبب:** منع مشغلي البوابات العاديين من تمرير بطاقات الأقسام المخالفة دون إذن المشرف.

### ADR-017: مبدأ الـ Fail-Closed في إنتاج قاعدة البيانات (Production Fail-Closed Architecture)
* **القرار:** في بيئة الإنتاج السحابية، في حال عدم توفر Supabase أو فشل الاتصال، يتم رمي خطأ صريح ورفض العملية فوراً، وحظر العودة للذاكرة المحلية.
* **السبب:** منع حدوث أي تشتت أو انقسام في بيانات الضيوف بين خوادم Vercel Serverless المتعددة.

### ADR-018: الحوكمة الصارمة لصلاحيات المشرفين في Supabase Auth
* **القرار:** عدم منح صلاحية المالك أو المشرف إلا للمستخدمين الذين يحملون صراحة دور `admin` أو يملكون معرف الفعالية في قاعدة البيانات.
* **السبب:** منع أي مستخدم مسجل في المنظومة من الوصول لبيانات ولوحة تحكم الفعاليات الأخرى.

### ADR-019: حظر استعلامات Data API المباشرة لجدول parties
* **القرار:** حذف سياسات القراءة العامة لجدول `parties` وحصر الوصول عبر خادم Next.js بواسطة `service_role`.
* **السبب:** حماية خصوصية الضيوف (الأسماء، الهواتف، الطاولات، الملاحظات) من الاستعلام المباشر عبر مفتاح anon.

### ADR-020: حصر تنفيذ RPC التحضير على service_role
* **القرار:** سحب صلاحية استدعاء `process_secure_checkin` من `authenticated` وقصرها على `service_role`.
* **السبب:** فرض التحقق الإلزامي من جلسة البوابة ورتبة المشرف عبر Next.js ومنع أي مستخدم مسجل من التلاعب بمعاملات الدخول.

### ADR-021: معمارية المستودعات المنفصلة (Repository Pattern Architecture)
* **القرار:** نقل كافة استعلامات قاعدة البيانات إلى `lib/repositories/supabase/SupabaseRepository.ts` واستخدام `MockRepository` في الاختبارات فقط.
* **السبب:** ضمان وجود مصدر وحيد للحقيقة في الإنتاج دون أي شروط fallback هجينة تضعف موثوقية النظام.

### ADR-022: تأمين مسار الفحص الميداني وحجب المقاييس التشغيلية
* **القرار:** حجب إحصائيات الحضور والضيوف عن `/api/health` العام ونقلها إلى `/api/admin/system-health` المحمي بجلسة المشرف.
* **السبب:** الالتزام بمبدأ Least Privilege ومنع تسريب بيانات الفعالية للعامة.

### ADR-023: التوليفة الطباعية الثلاثية واستراتيجية الواتساب المختصرة
* **القرار:** اعتماد خط `Alexandria` للعناوين الملكية، و `Amiri` للآيات، و `IBM Plex Sans Arabic` للأرقام والبيانات، مع اعتماد استراتيجية الرسائل المختصرة (The Prestigious Teaser) لرسائل الواتساب.
* **السبب:** تحقيق أعلى درجات الفخامة والوضوح لكافة الأعمار، ورفع نسبة فتح الروابط وتأكيد الحضور (RSVP) إلى أقصى حد ممكن.

### ADR-024: الحماية المشفرة للوحة الإدارة برمز المرور وجلسات المتصفح (Admin PIN Lock Screen)
* **القرار:** حجب واجهة `/admin` بالكامل خلف شاشة قفل مشفرة تطلب رمز PIN الإداري وتخزين حالة المصادقة في `sessionStorage` مع توفير خيار تسجيل الخروج وقفل الشاشة.
* **السبب:** منع أي وصول غير مصرح به لقائمة الضيوف والبيانات المالية وإعدادات الحفل عند مشاركة الرابط العام.

### ADR-025: التهيئة الذاتية لقاعدة البيانات السحابية ومنع أخطاء Single Row (Zero-Crash Auto-Seeding)
* **القرار:** استخدام استعلامات المصفوفات الآمنة والزرع التلقائي للفعالية الافتراضية في `SupabaseRepository` واستبدال كافة استدعاءات `.single()` المشددة بـ `.maybeSingle()` ومعالجات fallback.
* **السبب:** ضمان تحميل كافة صفحات السيرفر (SSR) باستقرار 100% دون كراش حتى لو كانت قاعدة البيانات جديدة أو خالية من الصفوف.

### ADR-026: عزل وتأمين كاش البطاقات ومنع التسريب عبر /api/join
* **القرار:** حذف معامل `offlineCache` تماماً من مسار `/api/join` وقصر تنزيل الكاش المحلي على مسار `/api/gate/cache` المحمي بجلسة البوابة المشفرة.
* **السبب:** سد ثغرة تسريب بيانات الضيوف وتجزئات التوكنات لأي مستخدم عام دون مصادقة.

### ADR-027: الاعتماد الحصري على كوكي HttpOnly وإلغاء حفظ التوكن في JavaScript
* **القرار:** حظر إرجاع `sessionToken` في جسم الـ JSON وحذف حفظه في `sessionStorage` بماسح البوابة، وقصر التحقق على كوكي `__Host-gate_session` (HttpOnly; Secure; SameSite=Strict).
* **السبب:** القضاء على خطر سرقة جلسات البوابات عبر ثغرات XSS وضمان مطابقة معايير OWASP.

### ADR-028: حظر الإدخال المباشر في RLS وفحص البايتات السحرية للصور
* **القرار:** إزالة سياسات `Public insert` في Supabase لـ `wishes` و `moments`، وفرض المرور عبر Next.js API مع فحص البايتات الثنائية (Magic Bytes) للصور المرفوعة.
* **السبب:** منع إغراق قاعدة البيانات (DDoS) وتجاوز الحجر الصحي، وصد هجمات رفع الملفات التنفيذية أو السكربتات الملغمة.

### ADR-029: مصادقة لوحة الإدارة على الخادم (DAL + Proxy Optimistic)
* **القرار:** حماية `/admin/*` بجلسة HMAC موقعة في كوكي `__Host-admin_session` ‏(HttpOnly/Secure/Strict) تُصدر من `/api/admin/auth`؛ فحص تفاؤلي في `proxy.ts` للتحويل السريع فقط، بينما `requireAdminSession()` في الـDAL يسبق أي استعلام، وكل handler في `/api/admin` يتحقق بنفسه ويطابق `session.eventId` مع الفعالية المُعدَّل عليها. لا علاقة لحماية الصفحة بحماية الـAPI.
* **السبب:** القفل العميلي السابق كان شكلياً — كامل بيانات الضيوف كانت تصل RSC payload لأي زائر. نمط DAL هو التوجيه الرسمي لـNext.js.

### ADR-030: الأوفلاين دخول مؤقت + Idempotency بالـqueueId
* **القرار:** المسح بدون إنترنت يمنح `LOCAL_ADMISSION` موسوماً بوضوح (وليس SUCCESS)، ويُصفّ بعنصر طابور يحمل queueId UUID + deviceId/sequenceNo/queuedAt؛ الـRPC يعيد النتيجة النهائية الأصلية حرفياً عند إعادة الإرسال، والحذف من الطابور فقط عند حكم نهائي، والتعارض يُعرض كتحذير. مطابقة الأوفلاين عبر هاش SHA-256 محلي (Web Crypto).
* **السبب:** localStorage قابل للتلاعب ولا يعلم بمسحات بوابات أخرى أثناء الانقطاع؛ ادعاء «تحقق نهائي» محلياً غير أمين أمنياً وتشغيلياً.

### ADR-031: قيود قاعدة البيانات واستثناء تجاوز المشرف
* **القرار:** فرض Invariants على مستوى الـschema‏ (فهرس فريد جزئي للتسجيل المكرر، CHECKs للأعداد والتنسيق) بنمط «تحقق ثم ABORT بتقرير» — إصلاح البيانات قرار بشري لا ترحيلة. `actual_checked_in_count > confirmed_count` مسموح عمداً (تجاوز المشرف لعدّ الوجبات) فلا يقيد.
* **السبب:** كل invariant يمكن فرضه في DB يجب أن يفرض فيها؛ خطأ كود مستقبلي لا يجب أن يفسد البيانات، واختيار «السجل الصحيح» ليس قرار schema.

### ADR-032: ADMIN_PIN مستقل دون تجزئة KDF
* **القرار:** رمز لوحة الإدارة متغير بيئة مستقل إلزامي بالإنتاج (≥16 حرفاً عشوائياً) مع guard يمنع تطابقه مع gate/supervisor PINs، ومقارنة constant-time. لم تُعتمد تجزئة KDF للـPIN لأن ملف البيانات يضم أصلاً `SUPABASE_SECRET_KEY` الشامل؛ التجزئة هنا تضيف تعقيداً دون خفض سطح الهجوم materially. ترقية مستقبلية مقبولة.
* **السبب:** فصل trust boundaries الثلاثة أهم من طول السر وحده، وبساطة مسار واحد تقلل الانحراف.

### ADR-033: Hardened CSP بنون عبر Proxy مع بوابة CSP_MODE
* **القرار:** ‏script-src ‏nonce+strict-dynamic يولدها proxy لكل طلب (تتطلب rendering ديناميكي — كل صفحاتنا الجوهرية force-dynamic أصلاً)، ‏style-src 'self' 'unsafe-inline' عملياً، والانتقال عبر `CSP_MODE=off|report-only|enforce`. استثناء موثق وحيد: /admin/stress-test الثابتة.
* **السبب:** ‏App Router يولّد inline scripts؛ ‏'self' المجردة تكسر hydration. التسمية «Hardened» لا «Strict» صادقة حول تساهل style-src.

### ADR-034: قناتا الاعتماد لـraw_pass_token وقناة الاسترجاع بحيازة الرابط
* **القرار:** التوكن الخام credential bearer يظهر فقط في: (1) ردود الإصدار (RSVP/التسجيل) و(2) `GET /i/[token]` حيث حيازة رابط الدعوة السري 128-bit نفسها اعتماد بثقة مكافئة — مخففات history/referrer/cache قائمة أصلاً (no-store + Referrer-Policy). محظور في admin/logs/cache/errors/health، ويثبته snapshot guards. فصل الاستعادة بقناة إضافية (SMS/رابط سحري) بند ما بعد الحفل.
* **السبب:** منع التوكن من GET الدعوة يكسر استعادة البطاقة الشائعة دون مكسب أمني (حيازة رابط الدعوة تتيح الوصول للبطاقة أصلاً بالتصميم ADR-001/002).
### 🔹 [v5.9.5] - 2026-08-23 (Public Data Boundary Hardening)

* **منع تسريب بيانات RSC/API العامة:** إضافة طبقة DTO خادمية بقوائم سماح صريحة قبل تمرير البيانات إلى واجهات العميل أو ردود RSVP والتسجيل والاسترجاع.
* **تغطية المسارات العامة كاملة:** حماية `/i/[token]` و`/join/[slug]` و`/moments` وصفحة البوابة، وإزالة رقم هاتف رافع الصورة من بيانات الألبوم العامة.
* **اختبار انحدار:** إضافة حارس آلي يمنع عودة `gate_pin` و`owner_id` وأرقام الجوال وهاشات الدعوات/البطاقات إلى حدود المتصفح.
